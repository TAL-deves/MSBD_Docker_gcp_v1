# version: "3.8"
# services:
#     nodeserver:
#         build:
#             context: ./Backend
#         ports:
#             - "8082:8082"
#     frontend:
#         build:
#             context: ./Frontend
#         ports:
#             - "80:80"
#             - "443:443"

version: "3.8"
services:
    nodeserver:
        build:
            context: ./Backend
        networks:
            - app-network
        ports:
            - "8082:8082"
    frontend:
        build:
            context: ./Frontend
        networks:
            - app-network
        ports:
            - "80:80"
networks:
    app-network: 

